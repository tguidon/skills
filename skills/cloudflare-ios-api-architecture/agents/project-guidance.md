## Cloudflare iOS API architecture

- Use `$cloudflare-ios-api-architecture` for Cloudflare-backed iOS API, persistence, authentication, or AI integration work.
- Read `docs/architecture-profile.md` when it exists.
- Treat the project profile and accepted architecture decision records as project-specific overrides.
- Keep provider-specific model and event details behind the Worker API.
